# Database configuration
database {
    host = "localhost"
    port = 5432
    name = "dms_meta"
    user = "dms"
    password = "dms"
    
    # Connection pool settings
    pool_size = 10
    max_overflow = 20
    pool_timeout = 30
    pool_recycle = 3600
} 